// RPM 系統 — 資料庫結構
// 本機開發使用 SQLite。日後部署到 Vercel + Supabase 時，
// 把 provider 改成 "postgresql"，url 改用 POSTGRES_PRISMA_URL，
// directUrl 改用 POSTGRES_URL_NON_POOLING（Vercel 的 Supabase 整合會自動填入）。

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

// ── 使用者（單人使用，僅由 seed 建立一個帳號）───────────
model User {
  id           String   @id @default(cuid())
  email        String   @unique
  name         String
  passwordHash String
  createdAt    DateTime @default(now())
}

enum Priority {
  MUST
  SHOULD
  COULD
}

enum TaskStatus {
  TODO
  DOING
  DONE
}

enum ResultStatus {
  ACTIVE
  DONE
  ARCHIVED
}

// ── RPM 條目：R（成果）+ P（理由）+ M（行動計畫，掛在底下的 Task）───
model Result {
  id String @id @default(cuid())

  title      String // R：具體、可衡量的成果
  purpose    String? // P：為什麼想要這個結果
  targetDate DateTime? // 時間：預計多久內完成

  status ResultStatus @default(ACTIVE)
  order  Int          @default(0)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  tasks Task[]
}

// ── 行動項目：M 的每一步，或未掛任何 RPM 條目的瑣碎待辦事項 ─────
// resultId 為 null 時就是純粹的待辦事項（不屬於任何 RPM 條目）。
model Task {
  id       String  @id @default(cuid())
  resultId String?
  result   Result? @relation(fields: [resultId], references: [id], onDelete: SetNull)

  title       String // 內容
  notes       String? // 備註
  priority    Priority   @default(SHOULD)
  status      TaskStatus @default(TODO)
  dueDate     DateTime? // 截止日期
  completedAt DateTime?
  order       Int        @default(0)

  createdAt DateTime @default(now()) // 建立日期
  updatedAt DateTime @updatedAt

  @@index([resultId])
  @@index([status])
  @@index([dueDate])
}

// ── 每日反思／心得（一天一筆自由文字）─────────────────────
model DailyNote {
  id      String   @id @default(cuid())
  date    DateTime @unique
  content String

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
